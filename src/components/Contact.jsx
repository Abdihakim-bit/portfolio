import React, { useState } from 'react';

export default function Contact() {
    return (
        <section id="contact" style={{ borderTop: '1px solid var(--border)', padding: '40px 0' }}></section>
    );
}